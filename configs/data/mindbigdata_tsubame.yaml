# ── Data configuration for H100 x4 ────────────────────────────────
#
# Larger batch size and more workers to saturate 4x H100 GPUs.
# Effective batch size = batch_size × 4 GPUs = 1024.
#

hdf5_dir: data/processed/digits_1_40hz
batch_size: 256
num_workers: 8
sfreq: 250
