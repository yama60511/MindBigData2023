# ── ATCNet Large — Scaled for H100 ─────────────────────────────────
_model_name_: atcnet

nb_classes: 10
channels: 128
samples: 500
lr: 3e-4

# ── Architecture (scaled up) ──
F1: 64
D: 2
kern_len: 64
n_windows: 5
tcn_channels: 128
n_heads: 4
dropout: 0.3
