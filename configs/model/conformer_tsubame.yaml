# ── EEG Conformer Large — Scaled for H100 ──────────────────────────
_model_name_: conformer

nb_classes: 10
channels: 128
samples: 500
lr: 1e-4

# ── Architecture (scaled up) ──
emb_size: 256
depth: 6
num_heads: 8
dropout: 0.3
