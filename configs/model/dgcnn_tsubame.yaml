# ── DGCNN Large — Scaled for H100 ──────────────────────────────────
_model_name_: dgcnn

nb_classes: 10
n_channels: 128
in_features: 5
lr: 3e-4

# ── Architecture (scaled up) ──
hidden_dim: 256
n_layers: 3
dropout: 0.3
